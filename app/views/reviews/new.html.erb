
<div class="container">
<h1>Create Rewiew</h1>
<%= form_for([@restaurant, @review]) do |f| %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <%= f.text_field :content %>
  <%= f.submit "submit", class:'btn btn-primary'%>
<% end %>
</div>
